# Expo version

This app targets **Expo SDK 54** (see `apps/mobile/package.json` — `expo: ~54.0.0`).

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. Do not apply guidance from a different SDK version and do not upgrade Expo or related packages without explicit approval.
