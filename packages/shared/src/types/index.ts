/**
 * @deprecated Use DTOs from `@caffeapp/shared/contracts` (camelCase API contracts).
 * Retained temporarily for mobile UI shell compatibility during Sprint 1 migration.
 */
export type { Role } from '../domain/session.types';
export { STAFF_ROLE_TO_MOBILE_ROLE, MOBILE_ROLE_TO_STAFF_ROLE } from '../domain/session.types';
